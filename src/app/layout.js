// IMPORTS
import './globals.css'

// LAYOUT
const Layout = ({ children }) => {

	// RENDER
  return (
    <html lang="de">
      <body>{children}</body>
    </html>
  );

};

// EXPORTS
export default Layout;
